<%= render @recipe %>

<div>
  <% if @user == current_user %>
    <%= link_to "Edit Recipe", edit_recipe_path(@recipe), class: 'edit-btn' %>
    <%= button_to "Delete Recipe", @recipe, method: :delete, class: 'delete-btn' %>
  <% end %>
</div>
